[gd_scene load_steps=3 format=2]

[ext_resource path="res://Assets/Sprites/AllyPlaceholder.png" type="Texture" id=1]

[sub_resource type="GDScript" id=1]
script/source = "extends \"res://Nodes/Combat/CombatStats.gd\"

var path = PoolVector2Array() setget set_path

var pos: Vector2

func _ready():
    set_process(false)

func _process(delta):
    var move_dist = speed * delta
    move_along_path(move_dist)

func move_along_path(distance: float):
    var start_point = pos
    for i in range(path.size()):
        var distance_to_next = start_point.distance_to(path[0])
        if distance <= distance_to_next and distance >= 0.0:
            pos = start_point.linear_interpolate(path[0], distance / distance_to_next)
            break
        elif path.size() == 1 and distance > distance_to_next:
            pos = path[0]
            set_process(false)
            handle_enemy_reaching_end()
        distance -= distance_to_next
        start_point = path[0]
        path.remove(0)

func handle_enemy_reaching_end():
    queue_free()

func set_path(value: PoolVector2Array):
    path = value
    if value.size() == 0:
        return
    set_process(true)
"

[node name="BaseEnemy" type="Node2D"]
script = SubResource( 1 )

[node name="Sprite" type="Sprite" parent="."]
modulate = Color( 1, 0.176471, 0.176471, 1 )
texture = ExtResource( 1 )

[node name="Caminho" type="Line2D" parent="."]
points = PoolVector2Array( -0.0303802, 0.282173 )
default_color = Color( 1, 0.14902, 0.905882, 1 )
